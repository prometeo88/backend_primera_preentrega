{
  "name": "primera-pre-entrega",
  "version": "1.0.0",
  "description": "primera pre entrega coderhouse - backend",
  "main": "app.js",
  "type": "module",
  "scripts": {
    "start": "node --watch ./src/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.19.2",
    "fs": "^0.0.1-security"
  }
}
